{% extends "base.jinja"%}

{% block content %}
        <h1> {{book.title}} </h1>

        <div class="book-container">

            <div class="book">
                <h2> {{ book.author }} </h2>
                <h3> {{ book.genre }} </h3>
            </div>
        </div>
{% endblock %}