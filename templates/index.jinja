<html>
    <head>
        <title> {{ title }} </title>
    </head>
    <body>
        <ul>
        {% for book in books %}
        <li>
        <h2>Title: {{ book.title }}</h2>
        <h3>Author: {{ book.author }}</h3>
        <h4>Genre: {{ book.genre }}</h4>
        {% endfor %}
        </li>
        </ul>
    </body>
</html>